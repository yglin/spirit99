(function() {
    'use strict';

    angular
        .module('spirit99')
        .service('Bookmark', Bookmark);

    Bookmark.$inject = ['UserCtrls', 'ChannelManager'];

    /* @ngInject */
    function Bookmark(UserCtrls, ChannelManager) {
        var self = this;

        ////////////////

    }
})();
